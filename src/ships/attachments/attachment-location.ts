export enum AttachmentLocation {
    front,
    frontRight,
    right,
    backRight,
    // NOTE: there is no 'back' attachment point due to location of thruster
    backLeft,
    left,
    frontLeft
}